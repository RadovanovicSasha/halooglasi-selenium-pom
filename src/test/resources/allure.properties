# Where Allure writes raw test results. Points under target/ (rather than
# allure-jupiter's project-root default) so results are treated as build
# output - cleaned by `mvn clean` and consistent with where the
# allure-maven plugin (mvn allure:report) looks for them by default.
allure.results.directory=target/allure-results
