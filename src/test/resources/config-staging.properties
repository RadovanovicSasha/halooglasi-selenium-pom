# Example staging environment configuration.
#
# This project targets the public production site (halooglasi.com) only -
# there is no real staging deployment. These are illustrative placeholder
# values demonstrating that the framework supports multiple environments
# via -Denv, not a live target. Running against this URL as-is will fail
# to connect; that's expected.
baseUrl=https://staging.example.com/
browser=chrome
timeout=10
