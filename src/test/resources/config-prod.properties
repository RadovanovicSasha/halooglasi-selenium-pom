# Production environment configuration for halooglasi.com.
# Real, non-sensitive values only - credentials are never stored here
# (see .env.example / EnvConfig).
baseUrl=https://www.halooglasi.com/
browser=chrome
timeout=10
